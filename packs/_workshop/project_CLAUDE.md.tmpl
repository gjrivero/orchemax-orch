# {{APP}} — product rules

This directory is a **git product** inside the workshop. Prefer shared layers for reusable code.

## Boot
1. Workshop root `CLAUDE.md` + `DEV_PRACTICES.md` / `GATES.md` (+ `doctrine/` if present)
2. This file
3. `docs/TRACKER.md` (status table)

## Placement
- Reusable → shared folder + lock
- Product-only → here
- Procedures → skills

Seeds are a kit, not a mandate.
